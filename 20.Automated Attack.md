# 20. 자동화 공격 (Automated Attack)

## 1. 개요 (Overview)
- **정의**: 웹 애플리케이션의 정상적인 기능(로그인, 게시글 작성, 결제 등)을 자동화된 도구(Bot, Script)를 사용하여 반복적으로 수행함으로써 시스템 부하를 유발하거나 부당한 이득을 취하는 취약점.
- **위험도**: **상 (High)** / 서비스 거부(DoS) 및 대규모 계정 탈취
- **영향**:
    - **Credential Stuffing**: 유출된 계정 정보를 대입하여 대규모 계정 탈취.
    - **서비스 오용**: 게시판 스팸 도배, 선착순 이벤트 매크로 독점, 재고 고갈 공격(Inventory Hoarding).
    - **리소스 고갈**: 과도한 트래픽으로 인한 서버 성능 저하 및 비용 증가.

> **💡 핵심 원리 (Volume & Velocity)**
>
> "사람은 1분에 로그인을 3번 시도하지만, 봇은 1분에 3,000번 시도한다. 서버가 이 둘을 구분하지 못하면 뚫린다."

---

## 2. 공격 메커니즘 (Attack Vector)

### 시나리오 1: 무차별 대입 & 크리덴셜 스터핑 (Brute Force & Stuffing)
**1. 딕셔너리 준비**
- 해커가 다크웹 등에서 구한 `ID:PW` 목록(Combo List)을 봇 프로그램에 로드함.

**2. 자동화 공격 수행**
- 봇이 프록시(IP 우회)를 사용해 웹사이트 로그인 API에 `POST /login` 요청을 쉼 없이 보냄.
- 캡차(CAPTCHA)나 임계치 설정이 없으면, 서버는 멍청하게 모든 요청에 대해 `True/False`를 응답해줌.

**3. 계정 선별**
- 공격자는 로그인에 성공한 계정만 따로 추출하여 2차 범죄(포인트 탈취, 정보 판매)에 이용함.

### 시나리오 2: 예외 처리 미흡을 이용한 DoS (Layer 7 DoS)
- 게시판 글쓰기, SMS 발송 등 DB 부하가 큰 기능을 자동화 툴로 무한 반복 호출.
- 특정 로직(예: 고용량 이미지 리사이징)을 집중 공략하여 CPU를 100%로 만듦.

---

## 3. 취약한 코드 vs 안전한 코드 (Java/Spring)

핵심은 **"사람인지 기계인지 구분(CAPTCHA)"**하고, **"너무 많이 요청하면 차단(Rate Limiting/Lock)"**하는 것입니다.

### ❌ 취약한 코드 (아무런 제한 없음)
> ID/PW가 맞는지 확인하는 로직만 있고, 몇 번 틀렸는지, 누가 요청했는지는 신경 안 씀.

```java
// LoginController.java
@PostMapping("/login")
public String login(@RequestParam String id, @RequestParam String pw) {
    // 취약점: 봇이 1초에 100번 틀려도 계속 받아줌
    if (memberService.checkPassword(id, pw)) {
        return "success";
    }
    return "fail"; // 틀렸다고 친절하게 바로 알려줌
}